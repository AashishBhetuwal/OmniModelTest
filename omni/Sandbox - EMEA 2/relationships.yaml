- join_from_view: superstore__orders
  join_to_view: superstore__customers
  join_type: always_left
  on_sql: ${superstore__orders.customer_id} = ${superstore__customers.id}
  relationship_type: many_to_one
  reversible: false

- join_from_view: superstore__orders
  join_to_view: superstore__products
  join_type: always_left
  on_sql: ${superstore__orders.product_id} = ${superstore__products.id}
  relationship_type: many_to_one
  reversible: false

- join_from_view: omni_dbt__orders
  join_to_view: omni_dbt__customers
  join_type: always_left
  on_sql: ${omni_dbt__orders.customer_id} = ${omni_dbt__customers.id}
  relationship_type: many_to_one
  reversible: false

- join_from_view: omni_dbt__orders
  join_to_view: omni_dbt__products
  join_type: always_left
  on_sql: ${omni_dbt__orders.product_id} = ${omni_dbt__products.id}
  relationship_type: many_to_one
  reversible: false
